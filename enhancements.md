To-dos to save likes, tweets, and retweets to local storage:

* Get tweets from localStorage on load
* Update tweets array instead of tweetsData
* Save tweets back after making changes
* Pass tweets array to render